<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">FindFood</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <%= menu 'Homepage', root_path %> 
      <%= menu 'Restaurants', restaurants_path %> 
      <%= menu 'Dishes', dishes_path %> 
      <%= menu 'Ingredients', ingredients_path %>
      <%= menu 'Categories', categories_path %>

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Filtrar
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% Category.order(:description).all.each do |category| %>
            <%= link_to category.description, homepage_restaurants_path(category_id: category.id), class:"dropdown-item"%>
          <% end %>
        </div>
      </li>
    </ul>
    
    <%= form_tag("/restaurants/homepage", method: 'post', :class => 'form-inline my-2 my-lg-0') do %>
    <%= text_field_tag(:search, nil, :class => 'form-control mr-sm-2') %>
    <div style="text-align: center;">
        <%= submit_tag("Buscar", :class => 'btn btn-outline-success my-2 my-sm-0') %>
      </div>
    <% end %>
  </div>
</nav>